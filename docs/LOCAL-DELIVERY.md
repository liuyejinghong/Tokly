# Tokens 本机开发版

2026-09-08。已安装到 `/Users/ethan/Applications/Tokens.app`，小号和中号正式组件已添加到桌面。主应用目前可在后台继续采集。旧的 TokensPrep 两张卡片仍是合成测试数据，未移除用户已有桌面项目。

## 使用

打开 Tokens 查看今日、近7天和自然月用量。客户端可展开到模型，模型详情保留客户端与所选范围。费用是按公开单价估算，星号表示部分Token缺价；未知费用不当作免费。

默认每10分钟采集，设置可选5分钟。关闭窗口仍由菜单栏运行，退出应用停止采集；退出后桌面组件保留最后成功值。主窗口筛选不改变组件的全部启用来源今日总量。来源选择和登录启动由用户控制，本轮未修改登录启动设置。

价格单独每日更新。首次价表下载成功后会补采以更新估值，不必等待下一轮定时采集。用户会话和项目内容保留本机，网络请求只用于公开价表。

## 已验证

- 现有Apple Development身份完成主应用、内置helper及Widget签名；安装包严格深度签名校验通过。
- 主应用与扩展解析到相同的团队App Group，实际共享文件读写成功。
- 原生窗口实际显示本机用量、部分估算费用及模型详情；刷新后价格生效。
- 正式小号/中号组件进入系统图库并添加到真实桌面，显示同一今日数据和采集时间。
- 主应用隐藏后，两张桌面组件从19:12数据更新为19:22数据；退出主应用后保留原数值。
- Collector48项、Shared103项、主应用96项、Widget59项自动检查通过；覆盖原口径对照、缺价/免费、时间边界、范围覆盖、取消/超时、损坏/缺失与权限错误。

## 验收范围

本机默认Codex、Claude Code、OpenCode语料完成口径对照；其常规替代目录也补了权限失败回归。其他客户端和自定义镜像配置未逐一认证，来源页有说明。

实际桌面验证使用当前深色外观。跨日、损坏和缺失场景有合成检查，没有等待真实午夜或强制本机睡眠；浅色桌面、真实睡眠恢复、长时间能耗和登录启动注册效果未在本轮实际验证。小组件点击深链代码和配置已检查，但桌面点击被当前辅助接口限制，未记为运行通过。系统掌握Widget刷新时机，不保证每5/10分钟准点显示。

这是macOS14+、Apple Silicon本机开发签名版，未做公开发行公证或Intel运行验收。

## 重建

运行 `bash scripts/check-collector.sh` 构建helper。复制 `Config/Local.xcconfig.example` 为已忽略的 `Config/Local.xcconfig`，填本机团队，再运行 `bash scripts/build-local.sh`。签名产物位于 `.build/TokensSigned/Build/Products/Debug/Tokens.app`。脚本使用现有开发身份，不自动申请新证书或公开发布。
