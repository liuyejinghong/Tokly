# Tokens macOS 需求与可行性

更新日期：2026-09-08。阶段：已完成首轮采集实测与架构文档；尚未实现正式应用，小组件实装待验。

## 已确认需求

- 做成本机 AI 用量统计应用，同时提供 macOS 顶部菜单栏与原生桌面小组件。
- 只统计这台设备的使用量；统计口径沿用 missuo/tokens，不自行重新定义 Token、缓存、推理、去重和日期归属规则。
- 费用按模型单价计算，不做订阅费用分摊或订阅权益换算。展示为估算费用。
- 不查询剩余额度。
- 支持安装包直接安装；Mac App Store 是否上架待后续评估，不阻塞直接发行。
- 希望每 5 或 10 分钟采集一次。首轮采集实测后的工程建议为默认 10 分钟、可选 5 分钟，应用常驻及电池测试仍待补做。

## 源码核实

研究快照：missuo/tokens 提交 `75aba190695c9de5f2695d9baba6afd3c8cb63f8`。

原版 `serve` 默认 30 分钟，可通过参数或环境变量调整。它每轮启动子进程，扫描、聚合并提交网站，然后等待周期加少量抖动。因此 30 分钟是服务默认的扫描及提交间隔，不是日志本身的记录频率，也不是核心库的限制。

核心 `tokens-core` 有扫描、解析、缓存、聚合、价格与 `generate_graph` 接口。原 CLI 已删除本地报表命令，需添加薄的本地 JSON 输出入口。核心存在来源报告费用与估算费用两种来源；实现时需确认按模型单价计费的路径，不能直接假设所有 `cost` 字段均为模型单价估算，也不能为费用调整改变 Token 统计口径。

## 实现方向

SwiftUI 主应用负责菜单栏、统计窗口和采集调度；内置短生命周期 Rust 辅助程序复用采集核心。主应用将最新统计快照原子写入共享容器，WidgetKit 扩展读取快照显示。菜单栏与小组件共用同一份统计结果，小组件不独立扫描日志。

采集完成后更新菜单栏，并请求 WidgetKit 刷新。采集周期与小组件实际显示刷新是两件事：WidgetKit 由系统调度，不能承诺每 5 或 10 分钟准时重绘。显示数据更新时间；需在正常系统设置下实测，不能用开发模式的刷新表现作为发行验收。

采用原生桌面小组件时，建议最低 macOS 14。主应用和扩展的 App Group、签名及直接发行安装后的识别和读取需要实际验证。直接发行不意味着扩展没有沙盒或签名要求。

主应用在后台运行时执行定时采集；关闭统计窗口继续运行，彻底退出则停止采集，小组件保留最近快照及时间。此生命周期为建议设计，尚未作为用户指定行为。

## 最小实测计划

1. 固定同一日志快照、价格数据与时区，对照原核心验证 Token、客户端、模型、日期聚合一致；费用验证按模型单价计算。
2. 分别测首次扫描、缓存命中的无变化扫描、持续新增日志扫描。记录耗时、CPU 时间、峰值内存、磁盘读写。
3. 用相近负载分别试跑 5 分钟和 10 分钟周期，记录应用常驻内存、唤醒、系统能耗指标；只用短跑资源指标时，不宣称已证明续航影响。
4. 验证扫描不重叠、失败后保留最近成功快照、睡眠期间不强制唤醒，恢复后补一次采集而非重放所有错过的周期。
5. 验证安装后桌面小组件可添加、共享快照一致、实际刷新延迟；覆盖主应用后台运行与退出场景。
6. 根据实测选择 5 或 10 分钟默认值；测试前不宣称某个周期低耗电。

## 依据

- [原版服务与提交入口](https://github.com/missuo/tokens/blob/75aba190695c9de5f2695d9baba6afd3c8cb63f8/cli/tokens-cli/src/main.rs)
- [原版核心](https://github.com/missuo/tokens/tree/75aba190695c9de5f2695d9baba6afd3c8cb63f8/cli/tokens-core/src)
- [Apple WidgetKit](https://developer.apple.com/documentation/widgetkit)
- [Apple 小组件刷新机制](https://developer.apple.com/documentation/widgetkit/keeping-a-widget-up-to-date/)
- [Apple App Groups](https://developer.apple.com/documentation/xcode/configuring-app-groups)

项目使用 MIT 许可；分发复用代码时保留原版权和许可声明，依赖与素材分别核对。

## 后续文档

- [首轮验证结果](docs/VALIDATION.md)
- [架构设计](docs/ARCHITECTURE.md)
- [Muse 开发任务包](docs/DEVELOPMENT.md)

首轮实测表明完整扫描的消息缓存能显著降低耗时；原版今日快速路径存在旧文件修改时间少计反例，首版不启用。统计口径仍以原版完整扫描为准。
